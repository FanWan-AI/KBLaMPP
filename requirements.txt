# KBLaM++ Plan B dependencies
# Base tooling and config
numpy>=1.25.0,<2
pyyaml>=6.0.1
typing-extensions>=4.7

# Core model stack
# Pin CUDA-enabled wheels so ``pip install -r`` doesn’t override the GPU build
torch==2.2.2+cu121
torchvision==0.17.2+cu121
torchaudio==2.2.2+cu121
transformers>=4.40.0,<5
sentencepiece>=0.1.99
huggingface-hub>=0.16.5
modelscope>=1.10.0

# Retrieval/quantisation helpers
faiss-cpu>=1.7.3
bitsandbytes==0.43.1
accelerate>=0.20.3,<1
peft>=0.5.2,<0.7

# Optional embedding helpers
sentence-transformers>=2.3.2